from fastapi import Form
from pydantic import BaseModel

# class InstructorForm(BaseModel):
#     course_name : str = Form(...) 
#     instructor_name : str = Form(...)

# class StudentForm(BaseModel):
#     name : str = Form(...)
#     email : str = Form(...)